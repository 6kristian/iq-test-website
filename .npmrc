# Suppress warnings about deprecated packages in transitive dependencies
# These are from dependencies of dependencies and will be resolved when upstream packages update
legacy-peer-deps=false
audit-level=moderate

